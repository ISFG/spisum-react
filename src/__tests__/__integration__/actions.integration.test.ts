import "actions";

describe("Test actions redundant ids", () => {
  test("Redundant actions ids", () => {
    // fake test only to load actions and check actions redundancy
    expect(true).toBeTruthy();
  });
});
