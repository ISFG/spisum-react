import styled from "@emotion/styled";

export const UnsupportedFileWrapper = styled("div")<{}>(() => ({
  alignItems: "center",
  display: "flex",
  height: "100%",
  justifyContent: "center",
  width: "100%"
}));
